/**
 * Loading (임시 placeholder)
 *
 * 참고 문서: docs/DESIGN_SYSTEM.md - 7. Interaction Principles (Loading)
 *
 * 페이지 전환 또는 데이터 로딩 중 표시되는 최소 로딩 상태.
 */
export default function Loading() {
  return <p>불러오는 중입니다...</p>;
}
